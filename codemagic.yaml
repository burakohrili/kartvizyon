workflows:
  kartvizyon-android-release:
    name: KartVizyon Android signed AAB
    max_build_duration: 60
    # main'e push edildiğinde otomatik çalışır. Bunun için GitHub deposunda
    # https://api.codemagic.io/hooks/<appId> adresine bakan bir webhook bulunmalıdır;
    # webhook yoksa Codemagic push'tan haberdar olmaz ve tetikleyici sessizce çalışmaz.
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true
    # Yalnız mobil uygulamayı veya CI tanımını etkileyen commit'lerde derle;
    # doküman ve web değişiklikleri mobil build dakikası harcamasın.
    when:
      changeset:
        includes:
          - "apps/mobile"
          - "codemagic.yaml"
    # Personal accounts receive free build minutes on the macOS M2 runner.
    # Linux X2 requires billing even for Android-only workflows.
    instance_type: mac_mini_m2
    working_directory: apps/mobile
    environment:
      groups:
        - mobile_runtime
      android_signing:
        - kartvizyon_upload
      vars:
        PACKAGE_NAME: app.kartvizyon.mobile
      # .github/workflows/ci.yml ile aynı sürüme sabitlenir. `stable` kayan bir
      # kanaldır; testlerin geçtiği sürümle mağazaya giden ikilinin sürümü
      # ayrışırsa release build'i hiç doğrulanmamış bir toolchain'le üretilir.
      # Nitekim stable 8.14+ Gradle istemeye başlayınca build kırıldı.
      # Flutter yükseltmesi ayrı ve test edilmiş bir değişiklik olmalıdır.
      flutter: 3.32.6
      java: 17
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $HOME/.gradle/caches
    scripts:
      - name: Create isolated Android signing configuration
        script: |
          cat > android/key.properties <<EOF
          storePassword=$CM_KEYSTORE_PASSWORD
          keyPassword=$CM_KEY_PASSWORD
          keyAlias=$CM_KEY_ALIAS
          storeFile=$CM_KEYSTORE_PATH
          EOF
      - name: Resolve dependencies
        script: flutter pub get
      - name: Static analysis
        script: flutter analyze
      - name: Unit and widget tests
        script: flutter test
      # $PROJECT_BUILD_NUMBER iki workflow'un TOPLAM çalışma sayısıdır; Android
      # ve iOS aynı sayacı paylaştığı için hiçbir mağazanın numarası ardışık
      # ilerlemiyordu ve Codemagic ekranındaki build indeksiyle de tutmuyordu
      # (Android çalışma #18 -> versionCode 34, iOS #21 -> build 43). Numara
      # mağazanın kendisinden gelirse her platform kendi dizisinde ilerler ve
      # konsolda görülen numara ile burada görülen numara aynı şeyi anlatır.
      - name: Resolve the next Play version code
        script: |
          LATEST=$(google-play get-latest-build-number \
            --credentials "$GCLOUD_SERVICE_ACCOUNT_CREDENTIALS" \
            --package-name "$PACKAGE_NAME")
          if [ -z "$LATEST" ]; then
            echo "Play'den son versionCode okunamadı; numara uydurmak yerine duruyoruz."
            exit 1
          fi
          echo "NEXT_BUILD_NUMBER=$((LATEST + 1))" >> "$CM_ENV"
          echo "Play'deki son versionCode $LATEST, bu build $((LATEST + 1))."
      - name: Build signed Android App Bundle
        script: |
          flutter build appbundle --release \
            --build-number=$NEXT_BUILD_NUMBER \
            --dart-define=KARTVIZYON_API_URL=https://app.kartvizyon.app \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=SENTRY_DSN=$SENTRY_DSN
    artifacts:
      - build/app/outputs/bundle/release/*.aab
      - build/app/outputs/mapping/release/mapping.txt
      - flutter_drive.log
    # iOS tarafı TestFlight'a kendi gidiyordu ama Android AAB elle indirilip
    # yükleniyordu. Play service account'u (yalnız KartVizyon'un sürüm iznine
    # sahip) bağlandığı için artık kapalı test kanalına doğrudan düşer.
    publishing:
      google_play:
        credentials: $GCLOUD_SERVICE_ACCOUNT_CREDENTIALS
        track: alpha
        # Sürüm taslak değil, doğrudan test kullanıcılarına açılır.
        submit_as_draft: false

  kartvizyon-android-fgs-video:
    name: KartVizyon Android FGS review video APK
    max_build_duration: 45
    # Bu workflow yalnız Codemagic arayüzünden elle çalıştırılır. Store'a paket
    # göndermez; Google Play ön plan hizmeti beyan videosu için production
    # Supabase oturumuyla çalışan, DEBUG etiketsiz bir profil APK üretir.
    instance_type: mac_mini_m2
    working_directory: apps/mobile
    environment:
      groups:
        - mobile_runtime
      flutter: 3.32.6
      java: 17
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $HOME/.gradle/caches
    scripts:
      - name: Resolve dependencies
        script: flutter pub get
      - name: Static analysis
        script: flutter analyze
      - name: Unit and widget tests
        script: flutter test
      - name: Build Supabase-configured profile APK
        script: |
          flutter build apk --profile \
            --dart-define=KARTVIZYON_API_URL=https://app.kartvizyon.app \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
    artifacts:
      - build/app/outputs/flutter-apk/app-profile.apk

  kartvizyon-ios-testflight:
    name: KartVizyon iOS signed IPA / TestFlight
    max_build_duration: 90
    # Otomatik tetiklemede `inputs` varsayılanları uygulanır; submitToTestFlight
    # varsayılan olarak false olduğu için push ile yalnız imzalı IPA üretilir,
    # TestFlight'a yükleme yapılmaz. Yükleme için build elle veya API ile
    # submitToTestFlight=true verilerek başlatılır.
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true
    when:
      changeset:
        includes:
          - "apps/mobile"
          - "codemagic.yaml"
    instance_type: mac_mini_m2
    working_directory: apps/mobile
    inputs:
      submitToTestFlight:
        description: Upload the verified build to TestFlight
        type: boolean
        # main'e giden her doğrulanmış build dahili test grubuna teslim edilir.
        # Grup şu an yalnız iç testçilerden oluştuğu için bildirim gürültüsü
        # yaratmaz. Dış test grubu açıldığında bu varsayılan tekrar false
        # yapılıp yükleme elle tetiklenmelidir.
        default: true
    integrations:
      app_store_connect: kartvizyon_app_store
    environment:
      groups:
        - mobile_runtime
      ios_signing:
        distribution_type: app_store
        bundle_identifier: app.kartvizyon.mobile
      vars:
        # get-latest-testflight-build-number sayısal Apple app kimliğini ister,
        # bundle identifier kabul etmez.
        APP_STORE_APP_ID: 6797552440
      # .github/workflows/ci.yml ile aynı sürüme sabitlenir. `stable` kayan bir
      # kanaldır; testlerin geçtiği sürümle mağazaya giden ikilinin sürümü
      # ayrışırsa release build'i hiç doğrulanmamış bir toolchain'le üretilir.
      # Nitekim stable 8.14+ Gradle istemeye başlayınca build kırıldı.
      # Flutter yükseltmesi ayrı ve test edilmiş bir değişiklik olmalıdır.
      flutter: 3.32.6
      xcode: latest
      cocoapods: default
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $HOME/Library/Caches/CocoaPods
    scripts:
      - name: Resolve dependencies
        script: flutter pub get
      - name: Static analysis
        script: flutter analyze
      - name: Unit and widget tests
        script: flutter test
      - name: Install CocoaPods dependencies
        script: find ios -name Podfile -execdir pod install \;
      - name: Apply KartVizyon-only iOS signing profiles
        script: xcode-project use-profiles --project ios/Runner.xcodeproj
      # Android tarafındaki gerekçenin aynısı; numara TestFlight'tan gelir,
      # böylece iOS sürümleri 43'ten 39'a atlamaz.
      - name: Resolve the next TestFlight build number
        script: |
          LATEST=$(app-store-connect get-latest-testflight-build-number "$APP_STORE_APP_ID")
          if [ -z "$LATEST" ]; then
            echo "TestFlight'tan son build numarası okunamadı; numara uydurmak yerine duruyoruz."
            exit 1
          fi
          echo "NEXT_BUILD_NUMBER=$((LATEST + 1))" >> "$CM_ENV"
          echo "TestFlight'taki son build $LATEST, bu build $((LATEST + 1))."
      - name: Build signed IPA
        script: |
          flutter build ipa --release \
            --build-number=$NEXT_BUILD_NUMBER \
            --export-options-plist=/Users/builder/export_options.plist \
            --dart-define=KARTVIZYON_API_URL=https://app.kartvizyon.app \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=SENTRY_DSN=$SENTRY_DSN
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: ${{ inputs.submitToTestFlight }}
